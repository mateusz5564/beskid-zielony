import styled from "styled-components";
import { Swiper } from "swiper/react";

const StyledSwiper = styled(Swiper)`
  img {
    height: 20rem;
  }
`;

const Styled = { StyledSwiper };

export default Styled;
